<!-- Validation failure messages -->
<% if @musician.errors.any? %>
  <% @musician.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Musician</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_musician" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for genre -->
      <div class="form-group">
        <label for="genre" class="control-label">
          Genre
        </label>

        <textarea id="genre" name="genre" placeholder="Genre" class="form-control" rows="3"><%= @musician.genre %></textarea>
      </div>

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          Name
        </label>

        <input type="text" id="name" name="name" placeholder="Name" class="form-control" value="<%= @musician.name %>">
      </div>

      <button class="btn btn-success">
        Create Musician
      </button>
      or
      <a href="/musicians">Cancel</a>
    </form>
  </div>
</div>
